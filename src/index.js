import { createRoot } from "react-dom/client";
// import BootstrapApp from "./Components/Bootstrap/App";
import { Company } from "./Components/Pages";
const root = createRoot(document.getElementById("root"));
root.render(<Company />);
